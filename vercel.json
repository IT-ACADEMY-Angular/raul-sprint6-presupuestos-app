{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist/presupuestos-app" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/" }
  ]
}
